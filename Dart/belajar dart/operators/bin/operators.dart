void main(List<String> arguments) {
  // Operator Assignment (=)

  // Operator Matematika (+; -, *, /, ~/, %)
  // Perbedaan / (menghasilkan output double) dan ~/ (menghasilkan output integer)
  print(2 / 3);
  print(2 ~/ 3);

  // Increment dan decrement
  int a = 3;
  a++;
  print("");
  print(a++);
  print(a--);
  print(a *= 2);
  print(a -= 2);

  // Operator Perbandingan (==, !=, <, >, <=, >=)

  // Operator Logika (&&, ||, !)
}
